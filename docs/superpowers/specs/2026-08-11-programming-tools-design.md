# 编程工具区设计

## 目标

在博客首页提供一个可快速访问在线编程环境的入口。

## 页面设计

在“职业测试”卡片之后添加一张与现有行动卡样式一致的“编程工具”卡片。卡片包含简短说明，以及一个在新标签页打开 `https://onecompiler.com` 的链接。

## 范围与验证

仅修改首页模板；不增加依赖、脚本或样式。通过 Jekyll 构建验证模板可正常生成，并检查链接地址、说明文案和新标签页安全属性。
